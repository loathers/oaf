steps:
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/oaf-js:$COMMIT_SHA', '-f', 'Dockerfile', '.']
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/oaf-js:$COMMIT_SHA']
# Deploy container image to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: ['app', 'deploy', 'oaf-js', '--image', 'gcr.io/$PROJECT_ID/oaf-js:$COMMIT_SHA', '--region', 'europe-west2']
images:
- gcr.io/$PROJECT_ID/oaf-js:$COMMIT_SHA